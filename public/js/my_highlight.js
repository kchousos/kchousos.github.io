// hljs.configure({
//     var lisp = hljs.requireLanguage("lisp").rawDefinition();
//     lisp.aliases.concat(["emacs-lisp"])
//     // push the new lisp back overtop of the old one
//     hljs.registerLanguage("lisp", lisp)
// })

hljs.initHighlighting();
